type notificationType = {
    avatar: string;
    titleKey: string;
    subtitleKey: string;
};

type profileType = {
    avatar: string;
    titleKey: string;
    subtitleKey: string;
    href: string;
};

type searchType = {
    titleKey: string;
    href: string;
};

export type { notificationType, profileType, searchType };
